name: Apply Stability Fixes V3

on:
  push:
    branches:
      - fix/stability-auto-answer-20260916
    paths:
      - '.github/workflows/apply-stability-fixes-v3.yml'

permissions:
  contents: write

jobs:
  apply-and-verify:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repair branch
        uses: actions/checkout@v4
        with:
          ref: fix/stability-auto-answer-20260916
          fetch-depth: 0

      - name: Apply deterministic source fixes
        run: python3 .github/scripts/apply-stability-fixes.py

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
          cache-dependency-path: ykt-helper/package-lock.json

      - name: Install dependencies
        working-directory: ykt-helper
        run: npm ci

      - name: Run full regression suite
        working-directory: ykt-helper
        run: npm test

      - name: Build userscript
        working-directory: ykt-helper
        run: npm run build

      - name: Prepare installable field-test userscript
        shell: bash
        run: |
          cp ykt-helper/dist/ykt-helper-1216.user.js release/ykt-helper-1216-stability-test.user.js
          python3 - <<'PY'
          from pathlib import Path
          p = Path('release/ykt-helper-1216-stability-test.user.js')
          text = p.read_text(encoding='utf-8')
          old = '// @version      1.21.6'
          new = '// @version      1.21.6.1'
          if text.count(old) != 1:
              raise SystemExit(f'Expected one version header, found {text.count(old)}')
          p.write_text(text.replace(old, new, 1), encoding='utf-8')
          PY
          test -s release/ykt-helper-1216-stability-test.user.js
          grep -q '// @version      1.21.6.1' release/ykt-helper-1216-stability-test.user.js
          grep -q 'autoAnswerEnabled' release/ykt-helper-1216-stability-test.user.js
          grep -q 'autoAnswerQueued' release/ykt-helper-1216-stability-test.user.js
          grep -q 'correction-failed' release/ykt-helper-1216-stability-test.user.js

      - name: Commit only verified fixes
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add ykt-helper/src release/ykt-helper-1216-stability-test.user.js
          git diff --cached --check -- ykt-helper/src
          git diff --cached --stat
          git commit -m "fix: restore reliable auto-answer scheduling"
          git push origin HEAD:fix/stability-auto-answer-20260916
